import styles from '../styles/ArtifactCard.module.css'
import { plex_sans } from '../fonts';
const ArtifactCard = () => {

    return (
        <div className={plex_sans.className}>
            <h1 className={styles.h1}>hello</h1>Artifact Card
        </div>
    );
};
export default ArtifactCard;
